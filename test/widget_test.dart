import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder — Firebase requires device/emulator to run',
      (WidgetTester tester) async {
    expect(1 + 1, 2);
  });
}
